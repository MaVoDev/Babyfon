ToDo:
==========================================================================
- Wenn im Einrichtungsass. die Backpressed gedrückt wird, nachfragen, ob der Assi beendet werden soll, wenn ja, zurück zur Übersicht
- Nur nach BabyfonModis suchen
- Broadcastsuche ausgeben

Allgemein:
- Service
- Design (Actionbarfarbe, Buttons, Navigation Drawer, ...)
- Methoden dem Modus anpassen (nur diese, die auch gebraucht werden, NavigationDrawer: nur Setup und Übersicht einblenden)

Notifications:
- Anrufe und SMS Anzahl ausgeben (Liste im Receiver definieren?)

Einstellungen:
- Mikrofonsensibilität

Connectivity:
- Audiostreaming
- CallReceiver so bauen, dass er als Receiver dient und automatisch bei einem neuen Miss Call sendet
- WiFi Direct State
- WiFi Direct Verbindung
- Bluetooth Verbindung

Setup:
- Nach Abschluss relevante Methoden starten (Battery, SMS, Call, ...)
- Navigation beim Einrichten (1,2,3)// siehe auch Mototroller Hilfe/Einrichtung, als Idee
- Wenn im Setup ein Schritt zurück gemacht wird, Werte zurück übergeben
- Wifi-Mode: IP und Name der gefundenen Geräte anzeigen (Suche funktioniert sc- Babymode nach Abschluss -> Info Fenster (Gerät wird jetzt in den baby mode versetzt) und Display automatisch aus.hon)

Optional:
==========================================================================
- Schlaflieder
- Sicherheit für die Verbindung Verschlüsseln
- Check, ob Flugmodus an/aus ist
- Anruf bei Geräuschen
- SMS bei Geräuschen
- Wenn Callfunction, dann Baby kann Mutter "anrufen", Lautsprecher an
- Display automatisch ausschalten

Für spätere Version:
==========================================================================
- Automatische Verbindungen
- Mehrere Geräte einbinden

Überlegungen:
==========================================================================
